# 🚀 뱀파이어 서바이버 + ZZZ 미니게임 스타일 웹 MVP 개발 계획서

## 1. Role & Objective
너는 정적 웹 배포(Static Web Hosting) 환경에 최적화된 게임을 개발하는 시니어 프론트엔드 아키텍트이자 게임 클라이언트 개발자다. 우리는 뱀파이어 서바이버 류의 '경험치 파밍 및 레벨업' 요소와 제한된 무기 슬롯(최대 5개)을 활용한 '무기 조합/강화' 시스템을 결합한 종스크롤 슈팅 미니게임 MVP를 내일까지 완성해야 한다. 

## 2. Tech Stack & Environment
*   **Build Tool:** Vite
*   **Language:** TypeScript (엄격한 타입 검사, `any` 지양)
*   **Rendering:** 인게임 로직(기체 이동, 적, 투사체, 보석, 충돌 판정)은 HTML5 `<canvas>` 사용 (엔진 미사용)
*   **UI/UX:** 레벨업 시 3선택지 카드, 체력바, 경험치바 등은 순수 HTML/CSS(DOM)로 구현하여 Canvas 위에 Overlay
*   **Data Structure:** 무기 스탯, 조합 레시피(Recipe), 웨이브 정보는 `GameConfig.ts`에 상수 객체나 JSON 형태로 완벽히 분리 (Data-Driven Architecture)

## 3. Core Architecture
*   **State & Render 분리:** 상태(유저 좌표, 적 배열, 무기 슬롯 상태, 경험치)를 관리하는 `GameState`와 렌더링을 담당하는 `Renderer`를 분리한다.
*   **오토 슈팅 시스템:** 플레이어는 마우스/터치 드래그로 기체의 좌표만 이동하며, 장착된 무기들은 각각의 쿨타임(데이터)에 맞춰 투사체를 자동 발사한다.

## 4. MVP Core Features

### A. 전투 및 파밍 루프 (In-Game Canvas)
*   **적 스폰 및 경고(Warning):**
    *   기본적으로 적은 화면 위에서 아래로 내려온다.
    *   화면 측면이나 하단에서 스폰되는 기습형 적의 경우, 화면 밖 보이지 않는 좌표에 투명한 적으로 2초간 대기시킨다. 
    *   대기하는 2초 동안 해당 스폰 위치 화면 끝단에 빨간색 `!` 경고 인디케이터를 렌더링한 후, 시간이 다 되면 실제 적으로 진입시킨다.
*   **경험치 보석:** 적이 죽으면 그 자리에 EXP 보석을 드롭하며, 플레이어가 접근해 획득하면 경험치 게이지가 찬다.

### B. 무기 슬롯 및 데이터 구조 (GameConfig.ts)
*   플레이어는 최대 5개의 무기 슬롯을 가진다.
*   무기 데이터는 3종의 베이스 무기(직사형, 방사형, 유도형), 이들을 2개 섞은 상위 무기, 상위 무기를 섞은 종결 무기 트리를 지원하도록 설계한다.

### C. 레벨업 및 강화 이벤트 (DOM Overlay)
*   경험치 바가 꽉 차면 캔버스의 `requestAnimationFrame` 루프를 일시정지(Pause)하고, HTML/CSS로 된 3선택지 UI를 띄운다.
*   **3선택지 추출 알고리즘 (가중치 기반 필터링):**
    1.  **조합(Merge):** 슬롯 내에 조합 조건을 만족하는 무기 2개가 있다면 최우선 가중치로 선택지에 등장. (선택 시 재료 무기 2개 삭제, 상위 무기 1개 추가)
    2.  **신규(New):** 슬롯이 5개 미만일 때, 미보유 하급 무기 등장.
    3.  **강화(Upgrade):** 장착 중인 무기의 레벨 업(스탯 증가).
    4.  **대성공(Jackpot):** 1~5%의 극히 낮은 확률로 '모든 무기 일괄 강화' 카드 등장.
*   선택 완료 시 다시 캔버스 루프를 재개(Resume)한다.

## 5. Execution Steps
명령을 받으면 다음 순서대로 작업을 진행하고, 각 단계가 끝날 때마다 나의 컨펌을 받아라.

1.  **Step 1:** 프로젝트 디렉토리 구조 설정 및 `GameConfig.ts`에 무기(직사/방사/유도), 레시피, 적 타입의 인터페이스(TypeScript Type)와 초기 목업 데이터를 작성해라.
2.  **Step 2:** `requestAnimationFrame` 기반의 게임 루프와 플레이어 기체 이동(마우스/터치 추적), 무기 자동 발사 코어를 구현해라.
3.  **Step 3:** 적 생성(측면/하단 Warning 로직 포함) 및 충돌 판정, 사망 시 경험치 보석 드롭 로직을 구현해라.
4.  **Step 4:** 경험치 MAX 도달 시 게임 일시정지 및 가중치 기반 3선택지를 화면에 뿌려주는 DOM Overlay UI(레벨업 시스템)를 구현해라.
5.  **Step 5:** 3선택지 중 하나를 골랐을 때 슬롯에 무기가 추가되거나 진화(합성)되는 데이터 변경 로직을 붙이고 게임을 재개해라.